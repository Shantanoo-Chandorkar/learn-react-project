const useStateInformation = [
  `useState is a Hook that lets you add React state to function components.`,
  `Syntax: const [state, setState] = useState(initialState);`,
  `The two variables in the array is always the state variable. The second variable is a updater function that updates the state variable. The naming for the updater function can be anything but the best practice is to add the 'set-' prefix to the state variable.`,
  `The initial state is the default value that a state can have. If you do not provide a value it will be 'undefined'.`,
];


export default useStateInformation;