const StateInformation = [
  `In simple terms, a "state" is the data that changes in time. In React specifically, it is a data that changes from one render to another render.`,
  `The example for a "state" is loading. Suppose, we are fetching a data from an API, we set the - let's say - showLoader state, which is initially set to false, to true until we receive a response from the API. Once we receive the response, we set the showLoader state back to false.`
]

export default StateInformation;